export default function AdminOrganizationsPage() {
  return (
    <div>
      <h1 className="text-[24px] font-semibold text-[var(--measured-text)]">
        Organizations
      </h1>
      <p className="mt-2 text-[13px] text-[var(--measured-subtext)]">
        Stage 8 owns this surface — orgs / users / patients / referrals /
        consent rolled into one admin console. Keeping the route alive in Stage
        7 so the Audit nav has a stable layout.
      </p>
    </div>
  );
}
