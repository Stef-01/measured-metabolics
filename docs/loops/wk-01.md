# Week 1 — Stage 1: Tri-shell scaffold

**Status:** GREEN · ship gate passed
**Stage:** 1 — rebrand, persona route groups, persona-aware navigation, mock fixtures
**Build dates:** 2026-05-22

## Build (Mon → Wed)

- Folder rename `Banksia → Measured` deferred — Windows file lock on the directory (Cursor's own indexer) wouldn't release. In-place rebrand: every code-side identifier is `measured` / `Measured Metabolics`. Disk folder remains `C:\Users\stefa\Banksia`. Documented in `README.md`.
- `package.json` rebumped to `measured@0.2.0-alpha.1`. Added: `zod`, `zustand`, `recharts`, `react-hook-form`, `@hookform/resolvers`, `@tanstack/react-query`, `vitest`.
- Token sweep: `--banksia-*` → `--measured-*` across CSS + 18 source files. Added `--measured-clinical-blue` / `--measured-clinical-amber` accents and three-tier `--measured-border-*` palette to `globals.css`. Added `.gp-sidebar` utility (360px column).
- Deleted Sous-era `(today)` route group and `components/today/`.
- Created tri-persona route groups: `(patient)/p`, `(dietitian)/d`, `(gp)/gp/[patientId]`.
- Created persona-specific navigation:
  - `components/patient/bottom-nav.tsx` — 5-tab bottom bar with `LayoutGroup` indicator
  - `components/dietitian/side-nav.tsx` — 240px left rail
  - `components/gp/sidebar-shell.tsx` — 360px column with patient header + 6-tab strip
- `lib/hooks/use-navigation.ts` — overloaded persona-aware hook + `gpSidebarTabs(patientId)` helper.
- `components/marketing/landing.tsx` — role chooser with 3 persona cards.
- `lib/mock/` — types + 4 patients (Asha, Ken, Lina, Omar) + meals + CGM (48h synth) + referrals + symptoms (Lina has `severe`) + messages + plans + recipes.
- `components/shared/persona-stub.tsx` — placeholder rendered by every Stage 1 stub page.
- 20 stub routes total (1 landing + 6 patient + 7 dietitian + 6 GP).

## Loop 1 (Thu → Fri) · red-team + fix

- **Red-team:** route every URL via `Invoke-WebRequest`. Including unknown patient `/gp/zzz/context` to prove `notFound()` fires.
- **Findings:**
  - 1 TS error: `useNavigation` returned a union of all `Tab<*>[]` types; the dietitian side-nav couldn't index `ICON_BY_TAB[tab.id]`. Fixed via three function overloads on `useNavigation`.
  - 1 stale `.next/dev/types/validator.ts` referencing the deleted `(today)/insights` route — cleared with `rm -rf .next`.
  - DeviceFrame originally framed every non-`/` route in a phone — wrong for `/d/*` and `/gp/*` which need full-bleed. Refactored to gate on `pathname.startsWith("/p/")`.
- **Fixes shipped:** all 3 above.

## Loop 2 (Sat → Sun) · edge-case + verify

- 4 prettier files reformatted via `pnpm lint:fix`.
- Approved-list smoke run:
  | check | result |
  |---|---|
  | `pnpm install` | clean (77 packages added) |
  | `pnpm lint` | clean |
  | `pnpm test` | 8/8 fixture invariants green |
  | `pnpm build` | 21 routes compiled (15 static, 6 dynamic) |
  | `pnpm dev` smoke 20 routes | 20 × 200 |
  | unknown GP patient `/gp/zzz/context` | 404 (`notFound()` fires) |

## Exit gate

- ✅ `pnpm build` clean
- ✅ `pnpm lint` clean
- ✅ All 19+ stub routes render under dev server
- ✅ Persona-aware nav highlights active tab on every route
- ✅ Mock fixtures cover the demo patient cohort (Asha, Ken, Lina, Omar)
- ✅ Vitest fixture invariants green
- ✅ Loop log filed

**Open notes for Week 2:**

- Patient `(patient)/p/plan/[slug]/page.tsx` recipe-detail route still pending — slot it in early next week.
- GP routes use `/gp/asha/*` as the canonical demo path; the role-chooser links there.
- `ToastHost` fixed at `bottom-20` — fine for patient PWA (above the bottom-nav), but on dietitian / GP surfaces it sits awkwardly. Address in Week 3 when those screens get real.

## Snapshot

```
src/app/
  (patient)/p/{home,meal,plan,metrics,symptoms,messages}/page.tsx
  (dietitian)/d/{dashboard,referrals,patients,queue,reports,messages}/page.tsx
  (dietitian)/d/patients/[id]/page.tsx
  (gp)/gp/[patientId]/{context,transcript,billing,care-plan,referral,report}/page.tsx
  layout.tsx + page.tsx + error.tsx + not-found.tsx + globals.css
```
