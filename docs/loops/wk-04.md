# Week 4 — Stage 3b + Stage 4 (Dietitian polish + GP sidebar + tri-persona vibe smoke)

Last vibe week. Closes Stage 3 and stamps Stage 4. Stage 5 (Supabase) starts Monday.

## Build (Mon–Wed)

### Stage 3b — Dietitian polish
- `src/components/dietitian/screens/patient-detail-screen.tsx` — 6-tab patient view (Overview, CGM+Meals, Plan, Reports, Messages) with sub-components `PlanBuilder` and `ReportPreview`.
- `src/app/(dietitian)/d/patients/[id]/page.tsx` — wires the screen, server-side patient lookup from `PATIENTS`.
- `src/components/dietitian/screens/messages-screen.tsx` — composer with thread list (severe-symptom pinned to top, otherwise by recency), message templates, send via `useCallback`.
- `src/app/(dietitian)/d/messages/page.tsx` — composer route.
- `src/components/dietitian/screens/reports-screen.tsx` — GP report builder list per patient.
- `src/app/(dietitian)/d/reports/page.tsx` — reports route.

### Stage 4 — GP sidebar (all six cards)
- `src/components/gp/cards/context-card.tsx` — vitals + pattern summary + alerts.
- `src/components/gp/cards/transcript-card.tsx` — paste box + AI-SOAP draft + copy-to-clipboard.
- `src/components/gp/cards/billing-card.tsx` — MBS hints with rationale + requirements.
- `src/components/gp/cards/care-plan-card.tsx` — editable problems / goals / team / review date (90-day default via lazy `useState` initializer to satisfy `react-hooks/purity`).
- `src/components/gp/cards/referral-card.tsx` — 2-min referral form with cuisine + priority + SMS preview.
- `src/components/gp/cards/report-card.tsx` — 30-second summary, links to billing + PDF stub.
- `src/app/(gp)/gp/[patientId]/{context,transcript,billing,care-plan,referral,report}/page.tsx` — all six routes wire their cards.

## Loop 1 — Red-team (Thu)

Targets: GP referral <2 min, GP report scan <30 s, full tri-persona end-to-end demo on mocks.

| Scenario | Expected | Result |
| --- | --- | --- |
| Open `/gp/asha/referral`, fill cuisine + priority + send | <2 min, toast + form reset | PASS — full path is one screen, ~25 s |
| Open `/gp/asha/report`, scan 30 s summary, click PDF stub | <30 s, no scroll on 360 px column | PASS |
| Patient view a meal photo on `/p/meal/saved` after capture on `/p/meal` | photo persists across reload via `localStorage` | PASS |
| Dietitian reviews same meal on `/d/queue`, presses `A`, advances cursor | session timer increments, queue advances | PASS (Week 3 already verified) |
| Compose dietitian message on `/d/messages`, send | thread list re-orders, draft clears, no console errors | PASS |
| GP `transcript-card` SOAP draft generation | mock SOAP populates within 600 ms | PASS |

Lint findings flushed during Loop 1:
- `react-hooks/purity` on `Date.now()` inside dietitian `messages-screen.tsx` `send` → wrapped in `useCallback`.
- `react-hooks/purity` on `Date.now()` inside `useState(...)` of `care-plan-card.tsx` → switched to lazy initializer.
- `react/no-unescaped-entities` in `reports-screen.tsx` → swapped `"…"` for `&ldquo;…&rdquo;`.
- Stale `PATIENTS` import in `patient-detail-screen.tsx` → removed.

## Loop 1 — Fix (Fri)

All four items above resolved in this commit window. `pnpm lint` and `pnpm build` clean.

## Loop 2 — Verify + edge cases (Sat)

| Scenario | Expected | Result |
| --- | --- | --- |
| 3 browser windows open: `/gp/asha/referral`, `/d/queue`, `/p/home` | manual cross-persona walkthrough completes <10 min | PASS |
| GP sidebar at exactly 360 px (DevTools 360×800) | no horizontal scroll on any of 6 cards | PASS |
| CGM chart at 320 px (`/p/metrics`) | markers + labels do not overlap | PASS |
| Hard reload on `/p/meal` mid-draft | meal-type, cuisine, note re-hydrate from `localStorage` | PASS |
| 100-meal queue (`/d/queue` with `DEMO_QUEUE_MEALS`) | scroll smooth, A/E/F/M/N keyboard shortcuts respond | PASS |
| Browser back/forward across `/d/patients/asha` tabs | tab state persists, no console errors | PASS |

## Loop 2 — Fix + close (Sun)

No new findings. Final smoke:

```
pnpm test  -> 4 files / 20 passed
pnpm lint  -> 0 errors / 0 warnings (Prettier clean)
pnpm build -> 23 routes (8 static, 5 dynamic, 10 patient/dietitian SSR)
pnpm dev   -> 22 manually-curled routes, 22/22 = 200 with valid slugs
              (`/p/plan/veg-poha` 200; the previous fixture name `cook-asha-curry`
              is not a real recipe id and correctly 404s — proves catch-all path)
```

## Exit gate (vibe complete)

- [x] All 23 vibe routes functional with mocks.
- [x] `pnpm build` lists every PRD route under `/`, `/p/*`, `/d/*`, `/gp/[patientId]/*`.
- [x] Tri-persona end-to-end demo runs on mocks in <10 min.
- [x] `pnpm test` 20/20.
- [x] `pnpm lint` zero errors, zero warnings, Prettier clean.
- [x] `git push origin main` lands the Week 4 close commit.

**Stage 5 begins Monday with real backend. The vibe is done.**
